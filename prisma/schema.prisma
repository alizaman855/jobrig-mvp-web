// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// ---------------------------------------------------------------------------
// Enums
// ---------------------------------------------------------------------------

enum Role {
  OWNER
  DISPATCHER
  TECH
}

enum JobStatus {
  NEW
  QUOTED
  SCHEDULED
  EN_ROUTE
  IN_PROGRESS
  COMPLETED
  PAID
}

enum QuoteStatus {
  DRAFT
  SENT
  SIGNED
}

enum InvoiceStatus {
  UNPAID
  PAID
}

enum ReviewRequestStatus {
  PENDING
  SENT
  DELIVERED
  FAILED
}

// ---------------------------------------------------------------------------
// Tenant root
// ---------------------------------------------------------------------------

/// The tenant. Every other model (except this one) carries a direct
/// businessId FK so every query can be scoped uniformly by lib/tenant.ts.
model Business {
  id              String   @id @default(cuid())
  name            String
  logoUrl         String?
  contactEmail    String?
  contactPhone    String?
  googleReviewUrl String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  users            User[]
  invitations      Invitation[]
  customers        Customer[]
  jobs             Job[]
  pricingTemplates PricingTemplate[]
  quotes           Quote[]
  quoteLineItems   QuoteLineItem[]
  invoices         Invoice[]
  reviewRequests   ReviewRequest[]
}

// ---------------------------------------------------------------------------
// Users
// ---------------------------------------------------------------------------

model User {
  id           String   @id @default(cuid())
  businessId   String
  name         String
  email        String   @unique
  passwordHash String
  role         Role
  serviceZone  String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  business           Business     @relation(fields: [businessId], references: [id], onDelete: Cascade)
  assignedJobs       Job[]        @relation("JobAssignedTech")
  invitationsSent    Invitation[] @relation("InvitationsSent")
  acceptedInvitation Invitation?  @relation("InvitationAccepted")

  @@index([businessId])
}

// ---------------------------------------------------------------------------
// Invitations
// ---------------------------------------------------------------------------

/// Invite-a-technician flow. tokenHash (not the raw token) is stored so a DB
/// read/leak can't be used to create an account; the raw token only ever
/// lives in the invite URL. acceptedByUserId links to the User created when
/// the invite is accepted — null until then, since that User doesn't exist yet.
model Invitation {
  id               String    @id @default(cuid())
  businessId       String
  email            String
  role             Role
  tokenHash        String    @unique
  invitedByUserId  String
  acceptedByUserId String?   @unique
  expiresAt        DateTime
  acceptedAt       DateTime?
  createdAt        DateTime  @default(now())

  business       Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  invitedBy      User     @relation("InvitationsSent", fields: [invitedByUserId], references: [id], onDelete: Cascade)
  acceptedByUser User?    @relation("InvitationAccepted", fields: [acceptedByUserId], references: [id], onDelete: SetNull)

  @@unique([businessId, email])
  @@index([businessId])
}

// ---------------------------------------------------------------------------
// Customers
// ---------------------------------------------------------------------------

model Customer {
  id         String   @id @default(cuid())
  businessId String
  name       String
  phone      String
  email      String?
  address    String
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  business       Business        @relation(fields: [businessId], references: [id], onDelete: Cascade)
  jobs           Job[]
  quotes         Quote[]
  invoices       Invoice[]
  reviewRequests ReviewRequest[]

  @@index([businessId])
}

// ---------------------------------------------------------------------------
// Jobs
// ---------------------------------------------------------------------------

model Job {
  id             String    @id @default(cuid())
  businessId     String
  customerId     String
  serviceType    String
  address        String
  status         JobStatus @default(NEW)
  notes          String?
  assignedTechId String?
  scheduledAt    DateTime?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  business       Business        @relation(fields: [businessId], references: [id], onDelete: Cascade)
  customer       Customer        @relation(fields: [customerId], references: [id], onDelete: Restrict)
  assignedTech   User?           @relation("JobAssignedTech", fields: [assignedTechId], references: [id], onDelete: SetNull)
  quotes         Quote[]
  invoices       Invoice[]
  reviewRequests ReviewRequest[]

  @@index([businessId])
  @@index([businessId, status])
  @@index([businessId, assignedTechId])
  @@index([businessId, scheduledAt])
}

// ---------------------------------------------------------------------------
// Pricing templates
// ---------------------------------------------------------------------------

/// Business-defined line items (e.g. "AC unit install" — $X per ton) that
/// the Quote Builder offers as one-tap-to-add options. Deliberately has no
/// relation to QuoteLineItem: a quote's line items are a copied snapshot
/// (description/unit/unitPrice) at the moment they're added, so editing or
/// deleting a template never changes a quote that already used it.
model PricingTemplate {
  id         String   @id @default(cuid())
  businessId String
  name       String
  unit       String
  unitPrice  Decimal  @db.Decimal(10, 2)
  sortOrder  Int      @default(0)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@index([businessId])
}

// ---------------------------------------------------------------------------
// Quotes
// ---------------------------------------------------------------------------

model Quote {
  id           String      @id @default(cuid())
  businessId   String
  jobId        String
  customerId   String
  status       QuoteStatus @default(DRAFT)
  total        Decimal     @default(0) @db.Decimal(10, 2)
  notes        String?
  signatureUrl String?
  signedByName String?
  signedAt     DateTime?
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  business  Business        @relation(fields: [businessId], references: [id], onDelete: Cascade)
  job       Job             @relation(fields: [jobId], references: [id], onDelete: Restrict)
  customer  Customer        @relation(fields: [customerId], references: [id], onDelete: Restrict)
  lineItems QuoteLineItem[]
  invoices  Invoice[]

  @@index([businessId])
  @@index([businessId, jobId])
  @@index([businessId, status])
}

model QuoteLineItem {
  id          String   @id @default(cuid())
  businessId  String
  quoteId     String
  description String
  unit        String?
  quantity    Decimal  @db.Decimal(10, 2)
  unitPrice   Decimal  @db.Decimal(10, 2)
  total       Decimal  @db.Decimal(10, 2)
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())

  business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  quote    Quote    @relation(fields: [quoteId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([businessId, quoteId])
}

// ---------------------------------------------------------------------------
// Invoices
// ---------------------------------------------------------------------------

/// @@unique([jobId]) is the actual duplicate-invoice guard — generation
/// always attempts a create and lets a unique-constraint violation mean
/// "already invoiced," rather than checking-then-creating (the same
/// read-then-write shape that let two quote signatures both succeed
/// earlier; see the fix for that in the git history for why).
model Invoice {
  id            String        @id @default(cuid())
  businessId    String
  jobId         String
  customerId    String
  quoteId       String?
  total         Decimal       @default(0) @db.Decimal(10, 2)
  status        InvoiceStatus @default(UNPAID)
  paymentMethod String?
  paidAt        DateTime?
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt

  business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  job      Job      @relation(fields: [jobId], references: [id], onDelete: Restrict)
  customer Customer @relation(fields: [customerId], references: [id], onDelete: Restrict)
  quote    Quote?   @relation(fields: [quoteId], references: [id], onDelete: Restrict)

  @@unique([jobId])
  @@index([businessId])
  @@index([businessId, status])
  @@index([businessId, status, paidAt])
}

// ---------------------------------------------------------------------------
// Review requests
// ---------------------------------------------------------------------------

/// twilioMessageSid stays reserved and unused for now — Phase 7 sends via
/// Resend email instead of Twilio SMS as a deliberate MVP deviation (no new
/// vendor account needed; Resend was already wired up in Phase 4.6). This
/// is not a permanent decision — flagged the same way as the deferred
/// invite-email and domain-verification items — so the column stays ready
/// for when SMS is actually wired up, and emailMessageId (the Resend
/// message id) is the field actually populated today.
///
/// @@unique([jobId]) is the same duplicate-send guard used for Invoice:
/// sending always attempts a create and treats a unique-constraint
/// violation as "already requested a review for this job," rather than a
/// separate check-then-create.
model ReviewRequest {
  id               String              @id @default(cuid())
  businessId       String
  jobId            String
  customerId       String
  status           ReviewRequestStatus @default(PENDING)
  sentAt           DateTime?
  twilioMessageSid String?
  emailMessageId   String?
  createdAt        DateTime            @default(now())

  business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  job      Job      @relation(fields: [jobId], references: [id], onDelete: Restrict)
  customer Customer @relation(fields: [customerId], references: [id], onDelete: Restrict)

  @@unique([jobId])
  @@index([businessId])
  @@index([businessId, status])
}
